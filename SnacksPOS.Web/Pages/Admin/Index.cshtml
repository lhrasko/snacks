@page
@model AdminModel
@{
    ViewData["Title"] = "Admin Dashboard";
}

<div class="container mx-auto px-4 py-8">
    <div class="max-w-2xl mx-auto">
        <div class="text-center mb-8">
            <h1 class="text-3xl font-bold mb-4 bg-gradient-to-r from-purple-600 to-blue-600 bg-clip-text text-transparent">
                🛠 Admin Dashboard
            </h1>
            <p class="text-lg opacity-80">Manage the entire system from here</p>
        </div>
        <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
            <a href="/Admin/Products" class="card text-center p-6 hover:shadow-lg transition">
                <div class="text-4xl mb-2">📦</div>
                <div class="font-semibold">Product Management</div>
            </a>
            <a href="/Admin/Inventory" class="card text-center p-6 hover:shadow-lg transition">
                <div class="text-4xl mb-2">📊</div>
                <div class="font-semibold">Inventory Management</div>
            </a>
            <a href="/Admin/Users" class="card text-center p-6 hover:shadow-lg transition">
                <div class="text-4xl mb-2">👥</div>
                <div class="font-semibold">User Management</div>
            </a>
            <a href="/Admin/Reports" class="card text-center p-6 hover:shadow-lg transition">
                <div class="text-4xl mb-2">📈</div>
                <div class="font-semibold">Reports</div>
            </a>
        </div>
    </div>
</div>
